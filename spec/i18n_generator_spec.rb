require 'spec_helper'

describe I18nGenerator do
  it 'has a version number' do
    expect(I18nGenerator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
